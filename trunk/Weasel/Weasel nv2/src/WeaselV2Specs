modifier class name {

	modifier type name;
	modifier type name = equalment;

	modifier classname(modifier paramType paramName){}

	modifier abstract type funcname(modifier paramType [paramName]);
	modifier type funcname(modifier paramType paramName){}

}

equalment:
0
a = equalment
new String[]{}


keywords:

abstract asm break catch class continue do else 
enum extends false final for if implements interface 
native new null private protected public return 
static super synchronized this throw true try while
